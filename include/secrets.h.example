/**
 * secrets.h.example - Template for secrets configuration
 *
 * Copy this file to secrets.h and fill in your actual values.
 * secrets.h is gitignored and will not be committed.
 */

#ifndef SECRETS_H
#define SECRETS_H

// WiFi credentials
#define WIFI_SSID "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"

// Soundbar Bluetooth name (shown in bluetoothctl as device name)
#define SOUNDBAR_NAME "ATS-1070 Yamaha"

// Soundbar Bluetooth MAC address (format: XX:XX:XX:XX:XX:XX)
// Used as fallback if name connection fails
#define SOUNDBAR_ADDRESS "00:00:00:00:00:00"

// Optional: API key for HTTP authentication (leave empty to disable)
#define API_KEY ""

// MQTT Broker settings
#define MQTT_HOST "homeassistant.local"
#define MQTT_PORT 1883
#define MQTT_USER ""
#define MQTT_PASSWORD ""

#endif
